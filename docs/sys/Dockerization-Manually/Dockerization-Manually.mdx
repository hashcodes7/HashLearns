---
id: dockerization-manually
title: Dockerization Manually
sidebar_label: Dockerization Manually
---

# Dockerization Manually

This documentation covers the manual process of dockerizing applications.

we have 2 spring projects so we are making 2 repositories , one for each. 

https://github.com/hashcodes7/smallkart_admin.git
https://github.com/hashcodes7/smallkart_customer.git

next we just run this command
``` bash
git init
```


then we connect it to our online github repo
``` bash
git remote add origin https://github.com/hashcodes7/smallkart_admin.git
```

verify
``` bash
git remote -v
```

expected result -
```
origin  https://github.com/hashcodes7/bigkart.git (fetch)
origin  https://github.com/hashcodes7/bigkart.git (push)
```

next we just go to left side source control and simply do commit and push

it will say
![image](/img/dockerization/Pasted%20image%2020260815025248.png)

just select ok and create a remote branch
if authentication is asked just authenticate and it will be pushed.

### Coz company pc i will use codespaces
![image](/img/dockerization/Pasted%20image%2020260815030310.png)

### set up codespaces jdk 
![image](/img/dockerization/Pasted%20image%2020260815115105.png)

### inside there use this confg so codespaces has jdk17 + docker both
``` json
{
  "name": "Java 17 & Docker Environment",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "features": {
    "ghcr.io/devcontainers/features/java:1": {
      "version": "17",
      "installMaven": "true",
      "installGradle": "true"
    },
    "ghcr.io/devcontainers/features/docker-in-docker:2": {
      "version": "latest",
      "enableMoby": "true"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "vscjava.vscode-java-pack",
        "ms-azuretools.vscode-docker"
      ]
    }
  }
}
```

### Then start your codespaces
![image](/img/dockerization/Pasted%20image%2020260815030501.png)

### here you will be presented with a vscode like interface
![image](/img/dockerization/Pasted%20image%2020260815030555.png)

we did this because we want to run docker, maven java etc commands which might not get installed in cognizant laptop


### first get the permission to use mvnw command
``` bash
chmod +x mvnw
```
run these to check your maven and java versions
``` bash
java -version
mvn -version
./mvnw -version
docker --version
```

expected result : just quick look through yours and dont compare line by line 
``` bash
openjdk version "17.0.20" 2026-07-21 LTS
OpenJDK Runtime Environment Microsoft-14670383 (build 17.0.20+8-LTS)
OpenJDK 64-Bit Server VM Microsoft-14670383 (build 17.0.20+8-LTS, mixed mode, sharing)
Apache Maven 3.9.16 (2bdd9fddda4b155ebf8000e807eb73fd829a51d5)
Maven home: /usr/local/sdkman/candidates/maven/current
Java version: 17.0.20, vendor: Microsoft, runtime: /usr/local/sdkman/candidates/java/17.0.20-ms
Default locale: en_US, platform encoding: UTF-8
OS name: "linux", version: "6.8.0-1052-azure", arch: "amd64", family: "unix"
Apache Maven 3.9.16 (2bdd9fddda4b155ebf8000e807eb73fd829a51d5)
Maven home: /home/vscode/.m2/wrapper/dists/apache-maven-3.9.16/56ba1f9f
Java version: 17.0.20, vendor: Microsoft, runtime: /usr/local/sdkman/candidates/java/17.0.20-ms
Default locale: en_US, platform encoding: UTF-8
OS name: "linux", version: "6.8.0-1052-azure", arch: "amd64", family: "unix"
Docker version 29.7.2-1, build a7dcaa6fdb6ed04aacbfdc76357fdae01605609e
```


once all is verified run this command to build jar file
``` bash
./mvnw clean package -DskipTests
```

### output
once jar is built you will see the jar file in target folder something like this 
![image](/img/dockerization/Pasted%20image%2020260815115948.png)

This is optional chapter to understand each like we put in the dockerfile

### Start with a empty java 17 Runtime
```
FROM eclipse-temurin:17-jre
```

### Creating a /app Folder inside image
```
WORKDIR /app
```

This way /app is now a location we can work with

### COPY Jar file inside image app folder
``` dockerfile
COPY target/bigkart-0.0.1-SNAPSHOT.jar app.jar
```

here we took the jar file and we put it inside our app folder so that its location becomes like this /app/app.jar

### Expose Port
``` Dockerfile
EXPOSE 8080
```
 DockerImage is a VDI , technically a software mimicing a whole computer system in of itself. So we have to expose a port in order to connect to this new virtual fake computer we created

### Selecting which jar to run
``` dockerfile
ENTRYPOINT ["java", "-jar", "app.jar"]
```

This tells the System that whenever docker image is run, start the app.jar file

A dockerFile which tells your system about how it should make the image, which jdk, which Linux os and everything it should use.

just quickly create it using touch command inside your codespaces itself
```
touch Dockerfile
```
![image](/img/dockerization/Pasted%20image%2020260815120337.png)

once done you will see a dockerfile built right in your project sidebar.

open that empty file and paste all this command there
``` dockerfile
FROM eclipse-temurin:17-jre

WORKDIR /app

COPY target/bigkart-0.0.1-SNAPSHOT.jar app.jar

EXPOSE 8080

ENTRYPOINT ["java", "-jar", "app.jar"]
```
save and close this file.

to check what each command does check this file [[4.1 (Optional) Understanding DockerFile Commands]]

- go to hub.docker.com
- make a account, set a username , choose free plan
- click create repository

![image](/img/dockerization/Pasted%20image%2020260815133804.png)

after creating it will look like this
![image](/img/dockerization/Pasted%20image%2020260815134031.png)

as it appears the repo is `hashcodes7/smallkart_customer`
so the docker app name- `hashcodes7/smallkart_customer:latest`
it has to be compatible






this can happen only after you have built the image using [[6-Building Docker Image]]
run 

``` bash
docker run -d --name smallkart-app -p 8080:8080 hashcodes7/smallkart_customer:latest
```
![image](/img/dockerization/Pasted%20image%2020260815160706.png)

note- if you use open in browser it will open :8080 port but we have made apis for our app so remove 8080 and enter the api 
eg 
`whatever whatever.github.dev/hello`
and you should see the app running
![image](/img/dockerization/Pasted%20image%2020260815162252.png)

use this command in github codespaces terminal to check logs
``` bash
docker logs -f smallkart-app
```

output will be like this 
![image](/img/dockerization/Pasted%20image%2020260815160916.png)


this will make a container in whichever machine you are running (codespace or ur own pc)
after testing you can delete the container so it doesnt take extra space

``` bash
docker stop smallkart-app
docker rm smallkart-app
```

next you can move to actual deployment to dockerhub on [[7-Logging in to DockerHub]]


lets say you created docker account and a repo, using [[5-creating dockerhub account & Repo]]
your full repo reference is `hashcodes7/smallkart_customer:latest`

Running this command in terminal makes a docker image from docker file. 
```
docker build -t hashcodes7/smallkart_customer:latest .
```

- docker build - builds an image
- -t bigkart - name of the image we waant
- repo reference `hashcodes7/smallkart_customer:latest` we put so Docker Hub knows where to push it. its optional while building image.
- . full stop in end means- take the full project and make this image

the output looks like this
![image](/img/dockerization/Pasted%20image%2020260815160335.png)

the docker image is created and is stored inside a hidden place in system

to see how we test docker image in a codespace before we actually push on docker hub check 
[[6.1 (optional) testing docker image locally before deploying]]

to directly move to deplyment go to [[7-Logging in to DockerHub]]



we have created docker repo and we have docker image in our system but we have not connected the two 
use this command

`docker login`
![image](/img/dockerization/Pasted%20image%2020260815163221.png)


click the link highlighted and open in browser with docker logged in already and enter code above
![image](/img/dockerization/Pasted%20image%2020260815163138.png)

confirm and continue
![image](/img/dockerization/Pasted%20image%2020260815163238.png)

![image](/img/dockerization/Pasted%20image%2020260815163305.png)

Login Succeeded means all done, now you can push into logged account's dockerhub's repo

use command in codespaces/local pc
`docker push appname` eg.
``` bash
docker push hashcodes7/smallkart_customer:latest
```

![image](/img/dockerization/Pasted%20image%2020260815163544.png)

done it is pushed verify it in dockerhub
![image](/img/dockerization/Pasted%20image%2020260815163657.png)

done its pushed to dockerhub
