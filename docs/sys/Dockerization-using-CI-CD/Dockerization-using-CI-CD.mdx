---
id: dockerization-using-ci-cd
title: Dockerization using CI/CD
sidebar_label: Dockerization using CI/CD
---

# Dockerization using CI/CD

This documentation covers the process of automating dockerization and deployment using CI/CD pipelines (e.g., GitHub Actions, GitLab CI).

we have 2 spring projects so we are making 2 repositories , one for each. 

https://github.com/hashcodes7/smallkart_admin.git
https://github.com/hashcodes7/smallkart_customer.git

next we just run this command
``` bash
git init
```


then we connect it to our online github repo
``` bash
git remote add origin https://github.com/hashcodes7/smallkart_admin.git
```

verify
``` bash
git remote -v
```

expected result -
```
origin  https://github.com/hashcodes7/bigkart.git (fetch)
origin  https://github.com/hashcodes7/bigkart.git (push)
```

next we just go to left side source control and simply do commit and push

it will say
![image](/img/dockerization/Pasted%20image%2020260815025248.png)

just select ok and create a remote branch
if authentication is asked just authenticate and it will be pushed.

- set up a cicd pipeline to build the jar file . copy template here 
https://github.com/hashcodes7/smallkart_customer/blob/master/.github/workflows/buildjar.yml

- set up a cicd pipeline to build the docker image of the jar file
https://github.com/hashcodes7/smallkart_customer/blob/master/.github/workflows/buildDockerImage.yml

but we have not connected our dockerhub to github yet. we need to do that by making a docker account (skip if done)

do that here [[4-creating dockerhub account]]






This is optional chapter to understand each like we put in the dockerfile

### Start with a empty java 17 Runtime
```
FROM eclipse-temurin:17-jre
```

### Creating a /app Folder inside image
```
WORKDIR /app
```

This way /app is now a location we can work with

### COPY Jar file inside image app folder
``` dockerfile
COPY target/bigkart-0.0.1-SNAPSHOT.jar app.jar
```

here we took the jar file and we put it inside our app folder so that its location becomes like this /app/app.jar

### Expose Port
``` Dockerfile
EXPOSE 8080
```
 DockerImage is a VDI , technically a software mimicing a whole computer system in of itself. So we have to expose a port in order to connect to this new virtual fake computer we created

### Selecting which jar to run
``` dockerfile
ENTRYPOINT ["java", "-jar", "app.jar"]
```

This tells the System that whenever docker image is run, start the app.jar file

A dockerFile which tells your system about how it should make the image, which jdk, which Linux os and everything it should use.

just quickly create it using touch command inside your codespaces itself
```
touch Dockerfile
```
![image](/img/dockerization/Pasted%20image%2020260815120337.png)

once done you will see a dockerfile built right in your project sidebar.

open that empty file and paste all this command there
``` dockerfile
FROM eclipse-temurin:17-jre

WORKDIR /app

COPY target/bigkart-0.0.1-SNAPSHOT.jar app.jar

EXPOSE 8080

ENTRYPOINT ["java", "-jar", "app.jar"]
```
save and close this file.

to check what each command does check this file [[4.1 (Optional) Understanding DockerFile Commands]]

- go to hub.docker.com
- make a account, set a username , choose free plan
- click create repository

![image](/img/dockerization/Pasted%20image%2020260815133804.png)

after creating it will look like this
![image](/img/dockerization/Pasted%20image%2020260815134031.png)

as it appears the repo is `hashcodes7/smallkart_customer`
so the docker image name should be `hashcodes7/smallkart_customer:latest`
it has to be compatible

now our dockerhub account and repo is created we are ready to push the image created by cicd pipeline into this repo. follow this file -





## Here we will generate Dockerhub PAT, and put it in Github Secrets
(you will not need this step in case you are doing manual deployment)

next we need a personal access t0ken along with our username to push into docker repo
![image](/img/dockerization/Pasted%20image%2020260815134443.png)

and now click personal t0ken and create one
![image](/img/dockerization/Pasted%20image%2020260815134543.png)

![image](/img/dockerization/Pasted%20image%2020260815134615.png)

This will give us the docker repo t0ken something like this
![image](/img/dockerization/Pasted%20image%2020260815155751.png)

take this t0ken and put it in github secrets
- go to github repo -> security
![image](/img/dockerization/Pasted%20image%2020260815134954.png)

- scroll down to secrets and variables and click github action 
- ![image](/img/dockerization/Pasted%20image%2020260815135247.png)
- create secret like this
- ![image](/img/dockerization/Pasted%20image%2020260815135340.png)
 ![image](/img/dockerization/Pasted%20image%2020260815135429.png)

| Secret key           | Value                           |
| -------------------- | ------------------------------- |
| `DOCKERHUB_USERNAME` | `hashcodes7`                    |
| `DOCKERHUB_TOKEN`    | Your Docker Hub PAT (above pic) |
next we can use them in our cicd pipeline

once your cicd pipeline pushes code you willget it automatically like this 
![image](/img/dockerization/Pasted%20image%2020260815135902.png)
